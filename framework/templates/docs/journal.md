# 行为流水

每个会话收尾追加一行:`日期 · 板块 · 任务 · 干了什么 · 产出在哪`。
Stop 闸门(闸门7)会在改过文件却没写这里时提醒一次。

最新的在最下面。

写法要点:
- **报号必带一句话内容**(`CLAUDE.md` 铁律5):写 `Q101` 就在同一句里写它是什么。
- **说完成要带证据**(铁律4):PR 号 / 合并后的 `origin/main` 提交 / 一条能复现那个数的命令。
- 塔被重开之前,最新一条本塔记录就是**接手须知**(做到哪、等谁、在跑的工人、排着的回查)。
- 只写这里,由 `node scripts/ledger-push.mjs "一句话"` 推上 `main`;工人不写台账。

---
